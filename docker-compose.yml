version: '3.8'

services:
  app: 
    container_name: designers-platform
    image: designers-platform
    build: 
      context: .
      target: development
    volumes:
      - ./src:/app/srс
    ports:
      - 80:3000
